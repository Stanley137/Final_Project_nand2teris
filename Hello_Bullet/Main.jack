class Main {
    function void main() {
        var Bullet bullet;
        var Bullet enemy_bullet;

        let bullet = Bullet.new(100, 100, 1);
        let enemy_bullet = Bullet.new(200, 50, -1);

        do bullet.move_right();
        do enemy_bullet.move_left();
        return;
    }
}