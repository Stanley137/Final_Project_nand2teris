class Bullet {
    field int x;
    field int y;
    field int dir;
    constructor Bullet new(int x,int y,int dir){
        let this.x = x;
        let this.y = y;
        let this.dir = dir;
        
    }
    method void right(int x,)
    method void move(int x,int y,int dir){
        // dir == 1, move right, dir == -1 move left
        if(dir = 1){

        }
    }
}