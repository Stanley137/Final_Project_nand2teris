class Main {
    function void main() {
        var Game game;
        // level 1
        let game = Game.new(); 
        do game.init(1, 5, 4, 1); // (level, jet_hp, num_enemy, enemy_hp)
        return;
    }
}